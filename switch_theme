#!/bin/bash
# Invoked by dark-mode-notify (see setup_theme_sync) when macOS switches
# between light and dark. The active theme is whatever theme-set last chose;
# re-applying it picks the variant matching the new appearance.

exec "${DOTFILES:-$HOME/.dotfiles}/theme-set" --reload
